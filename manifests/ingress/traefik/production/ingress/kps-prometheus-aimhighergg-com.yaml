apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kps-prometheus-aimhighergg-com
  namespace: monitoring
  annotations:
    ingressClassName: traefik
    cert-manager.io/cluster-issuer: letsencrypt-prod-dns01
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: monitoring-ip-whitelist@kubernetescrd,monitoring-basic-auth@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - kps-prometheus.aimhighergg.com
      secretName: kps-prometheus-aimhighergg-com-tls
  rules:
    - host: kps-prometheus.aimhighergg.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kube-prometheus-stack-prometheus
                port:
                  number: 9090
