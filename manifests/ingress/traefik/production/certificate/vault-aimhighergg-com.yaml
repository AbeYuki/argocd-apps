apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-aimhighergg-com-tls
  namespace: vault
spec:
  secretName: vault-aimhighergg-com-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod-dns01
  dnsNames:
    - vault.aimhighergg.com
