apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nextcloud-aimhighergg-com-tls
  namespace: nextcloud
spec:
  secretName: nextcloud-aimhighergg-com-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod-dns01
  dnsNames:
    - nextcloud.aimhighergg.com
