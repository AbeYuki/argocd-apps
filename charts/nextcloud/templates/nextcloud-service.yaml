apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextcloud.fullname" . }}-nextcloud
  labels:
    {{- include "nextcloud.labelsWithComponent" (dict "component" "nextcloud" "context" .) | nindent 4 }}
spec:
  type: {{ .Values.nextcloud.service.type }}
  ports:
    - port: {{ .Values.nextcloud.service.port }}
      targetPort: {{ .Values.nextcloud.service.port }}
      protocol: TCP
  selector:
    {{- include "nextcloud.selectorLabelsWithComponent" (dict "component" "nextcloud" "context" .) | nindent 4 }}
