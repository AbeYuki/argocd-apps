{{- if or .Values.server.secret.annotations .Values.server.secret.stringData }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "immich.serverConfigName" . }}
  labels:
    {{- include "immich.labelsWithComponent" (dict "component" "server" "context" .) | nindent 4 }}
  {{- with .Values.server.secret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
{{- with .Values.server.secret.stringData }}
stringData:
{{- range $key, $val := . }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
{{- end }}
{{- end }}
