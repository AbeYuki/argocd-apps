apiVersion: v1
kind: Secret
metadata:
  name: {{ include "immich.postgresSecretName" . }}
  labels:
    {{- include "immich.labelsWithComponent" (dict "component" "postgres" "context" .) | nindent 4 }}
  {{- with .Values.postgres.secret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
{{- range $key, $val := .Values.postgres.secret.stringData }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
